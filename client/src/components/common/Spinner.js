import React from "react";

const Spinner = () => {
  return (
    <div className="w-full flex items-center justify-center">
      <i className="fas fa-circle-notch fa-lg animate-spin "></i>
    </div>
  );
};

export default Spinner;
